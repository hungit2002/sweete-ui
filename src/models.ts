export interface UserInfoLS {
    id: number;
    fullname: string;
    email: string;
    avatar: string;
    phone: string;
}

export interface FEED_STATUS {
    icon: any,
    label: string,
    detail?: string,
    id: string,
    value: string
}